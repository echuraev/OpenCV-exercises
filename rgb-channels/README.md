# Level 2. Split Image on RGB channels

Create four windows with initial image and red, green, blue channels of the
image.

## Building and running
```bash
mkdir build && cd build
cmake .. && make
./simple
```
